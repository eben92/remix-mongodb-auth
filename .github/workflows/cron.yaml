name: daily-cron
on:
  schedule:
    - cron: '0 1 * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Add a job
        run: |
          curl --request POST \
          --url 'https://remix-mongodb-auth.vercel.app/hello' \
